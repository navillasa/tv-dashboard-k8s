apiVersion: v1
kind: Service
metadata:
  name: grafana-monitoring
  namespace: tv-dashboard-prod
spec:
  type: ExternalName
  externalName: grafana.monitoring.svc.cluster.local
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
