apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "tv-dashboard"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "tv-dashboard"
          serviceAccountRef:
            name: "dev-external-secrets-sa"
            namespace: "tv-dashboard-dev"
